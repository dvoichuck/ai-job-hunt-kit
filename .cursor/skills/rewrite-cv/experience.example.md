# Ground-truth (local only)

Copy to `experience.md` in this folder. That file is gitignored.

- Name, contacts, salary, stack, employers — only here and in `local/profile.env`.
- Do not invent facts. Unknown → ask or `[TODO]`.
